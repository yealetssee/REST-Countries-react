export type modeType = {
  activeTheme: string;
};

export type propsType = {
  setActiveTheme: Function;
  activeTheme: string;
};

export type dropType = {
  activeTheme: string;
  isShown: boolean;
};
